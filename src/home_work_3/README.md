Implementation of a recursive-descent parser for a subset of a context-free grammar of an arithmetic calculator.

Usage: provide an expression to calculate in quotation marks:
```
java Calc "4.1 + 15 * 7 + ( 28 / 5 ) ^ 2"
```
